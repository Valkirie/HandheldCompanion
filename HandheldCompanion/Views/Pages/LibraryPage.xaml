<Page
    x:Class="HandheldCompanion.Views.Pages.LibraryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:l="clr-namespace:HandheldCompanion.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:HandheldCompanion.Properties"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:viewmodels="clr-namespace:HandheldCompanion.ViewModels"
    Name="About"
    Title="{l:Static resx:Resources.MainWindow_navLibrary}"
    d:Background="White"
    d:DataContext="{d:DesignInstance Type=viewmodels:LibraryPageViewModel}"
    d:DesignHeight="1000"
    d:DesignWidth="1000"
    KeepAlive="True"
    mc:Ignorable="d">

    <Grid Margin="20">

        <Rectangle
            x:Name="BackgroundRect"
            Width="1200"
            HorizontalAlignment="Right"
            VerticalAlignment="Stretch"
            Opacity="0">
            <Rectangle.Fill>
                <ImageBrush ImageSource="{Binding Artwork, Mode=OneWay, UpdateSourceTrigger=PropertyChanged, NotifyOnTargetUpdated=True}" Stretch="None" />
            </Rectangle.Fill>

            <Rectangle.OpacityMask>
                <RadialGradientBrush Center="0.5,0.5" GradientOrigin="0.5,0.5" RadiusX="1.25" RadiusY="0.7">
                    <GradientStop Offset="0.0" Color="{DynamicResource MaskGradientStartColor}" />
                    <GradientStop Offset="0.4" Color="{DynamicResource MaskGradientEndColor}" />
                </RadialGradientBrush>
            </Rectangle.OpacityMask>

            <Rectangle.Triggers>
                <EventTrigger RoutedEvent="Binding.TargetUpdated">
                    <BeginStoryboard>
                        <Storyboard>
                            <DoubleAnimation
                                Storyboard.TargetProperty="Opacity"
                                From="0"
                                To="0.2"
                                Duration="0:0:0.3" />
                        </Storyboard>
                    </BeginStoryboard>
                </EventTrigger>
            </Rectangle.Triggers>
        </Rectangle>

        <ikw:SimpleStackPanel Spacing="12">
            <!--  Library  -->
            <TextBlock Style="{StaticResource BaseTextBlockStyle}" Text="Library" />
            <ikw:SimpleStackPanel Spacing="6">
                <ui:ItemsRepeater ItemsSource="{Binding Profiles}">
                    <ui:ItemsRepeater.Layout>
                        <ui:UniformGridLayout
                            ItemsStretch="Uniform"
                            MaximumRowsOrColumns="8"
                            MinColumnSpacing="12"
                            MinItemHeight="240"
                            MinItemWidth="160"
                            MinRowSpacing="12"
                            Orientation="Horizontal" />
                    </ui:ItemsRepeater.Layout>

                    <ui:ItemsRepeater.ItemTemplate>
                        <DataTemplate>
                            <Button
                                x:Name="ImageContainer"
                                Padding="0"
                                BorderThickness="0"
                                Command="{Binding Navigate}"
                                GotFocus="ImageContainer_GotFocus"
                                LostFocus="ImageContainer_LostFocus"
                                MouseEnter="ImageContainer_MouseEnter"
                                MouseLeave="ImageContainer_MouseLeave"
                                Tag="{Binding}">
                                <Button.RenderTransform>
                                    <TranslateTransform />
                                </Button.RenderTransform>

                                <Button.Triggers>
                                    <EventTrigger RoutedEvent="MouseEnter">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                    To="-3"
                                                    Duration="0:0:0.1" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                    <EventTrigger RoutedEvent="MouseLeave">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation
                                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                    To="0"
                                                    Duration="0:0:0.1" />
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Button.Triggers>

                                <Grid>
                                    <Image
                                        x:Name="MainImage"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Source="{Binding Cover, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                        Stretch="UniformToFill" />

                                    <Image
                                        x:Name="BlurBottomImage"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center"
                                        Source="{Binding Cover, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
                                        Stretch="UniformToFill">
                                        <Image.Effect>
                                            <BlurEffect Radius="10" />
                                        </Image.Effect>

                                        <Image.OpacityMask>
                                            <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                                                <GradientStop Offset="0.65" Color="Transparent" />
                                                <GradientStop Offset="0.85" Color="Black" />
                                            </LinearGradientBrush>
                                        </Image.OpacityMask>
                                    </Image>

                                    <TextBlock
                                        Margin="12"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Bottom"
                                        Foreground="White"
                                        Style="{StaticResource BaseTextBlockStyle}"
                                        Text="{Binding Name}"
                                        TextWrapping="Wrap" />
                                </Grid>
                            </Button>
                        </DataTemplate>
                    </ui:ItemsRepeater.ItemTemplate>
                </ui:ItemsRepeater>
            </ikw:SimpleStackPanel>

            <!--  Settings  -->
            <TextBlock Style="{StaticResource BaseTextBlockStyle}" Text="Settings" />
            <ikw:SimpleStackPanel Spacing="6">
                <ui:SettingsCard Description="Test" Header="Test" />
            </ikw:SimpleStackPanel>
        </ikw:SimpleStackPanel>
    </Grid>
</Page>