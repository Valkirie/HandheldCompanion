<Page
    x:Class="HandheldCompanion.Views.Pages.HotkeysPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:HandheldCompanion.Behaviors"
    xmlns:cmdHC="clr-namespace:HandheldCompanion.Commands.Functions.HC"
    xmlns:cmdWIN="clr-namespace:HandheldCompanion.Commands.Functions.Windows"
    xmlns:converters="clr-namespace:HandheldCompanion.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:l="clr-namespace:HandheldCompanion.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:resx="clr-namespace:HandheldCompanion.Properties"
    xmlns:sys="clr-namespace:System;assembly=mscorlib"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:viewmodels="clr-namespace:HandheldCompanion.ViewModels"
    Name="Hotkeys"
    Title="{l:Static resx:Resources.HotkeysPage_Hotkeys}"
    d:Background="White"
    d:DataContext="{d:DesignInstance Type=viewmodels:HotkeyPageViewModel}"
    d:DesignHeight="1500"
    d:DesignWidth="1000"
    KeepAlive="True"
    mc:Ignorable="d">

    <Page.Resources>
        <!--  Converters to split the list  -->
        <converters:EvenIndexFilterConverter x:Key="EvenIndexFilter" />
        <converters:OddIndexFilterConverter x:Key="OddIndexFilter" />
    </Page.Resources>

    <StackPanel>
        <ikw:SimpleStackPanel Spacing="28">
            <!--  Hotkeys settings  -->
            <ikw:SimpleStackPanel Spacing="8">
                <DockPanel>
                    <TextBlock
                        VerticalAlignment="Center"
                        Style="{StaticResource BaseTextBlockStyle}"
                        Text="{l:Static resx:Resources.HotkeysPage_HotkeysSettings}" />
                    <TextBlock
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                        Text="Global options for all hotkey actions" />
                </DockPanel>

                <ikw:SimpleStackPanel Spacing="3">
                    <!--  Rumble on execution  -->
                    <ui:SettingsCard Description="{l:Static resx:Resources.HotkeysPage_RumbleOnExecutionDesc}" Header="{l:Static resx:Resources.HotkeysPage_RumbleOnExecution}">
                        <ui:SettingsCard.HeaderIcon>
                            <ui:FontIcon Glyph="&#xE877;" />
                        </ui:SettingsCard.HeaderIcon>

                        <ui:ToggleSwitch IsOn="{Binding Rumble, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </ui:SettingsCard>

                    <ui:SettingsCard Description="{l:Static resx:Resources.HotkeysPage_RumbleStrengthDesc}" Header="{l:Static resx:Resources.HotkeysPage_RumbleStrength}">
                        <ui:SettingsCard.HeaderIcon>
                            <ui:FontIcon Glyph="&#xE877;" />
                        </ui:SettingsCard.HeaderIcon>

                        <DockPanel>
                            <TextBox
                                Height="31"
                                HorizontalAlignment="Right"
                                VerticalAlignment="Center"
                                HorizontalContentAlignment="Center"
                                IsReadOnly="True"
                                Text="{Binding Value, StringFormat={}{0:N0}%, Source={x:Reference SliderRumbleStrength}, Mode=OneWay}" />
                            <Slider
                                x:Name="SliderRumbleStrength"
                                Width="200"
                                Margin="6,0,0,0"
                                IsMoveToPointEnabled="True"
                                IsSnapToTickEnabled="True"
                                LargeChange="10"
                                Maximum="100"
                                Minimum="0"
                                SmallChange="1"
                                TickFrequency="1"
                                ToolTip="{Binding Value, StringFormat={}{0:N0}%, RelativeSource={RelativeSource Self}, Mode=OneWay}"
                                Value="{Binding RumbleStrength, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                        </DockPanel>
                    </ui:SettingsCard>
                </ikw:SimpleStackPanel>
            </ikw:SimpleStackPanel>

            <!--  Hotkeys  -->
            <ikw:SimpleStackPanel Spacing="8">
                <DockPanel>
                    <TextBlock
                        VerticalAlignment="Center"
                        Style="{StaticResource BaseTextBlockStyle}"
                        Text="{l:Static resx:Resources.HotkeysPage_Hotkeys}" />
                    <TextBlock
                        Margin="4,0,0,0"
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                        Text="Create and manage button combination shortcuts" />
                </DockPanel>

                <ikw:SimpleStackPanel Spacing="3">
                    <ui:SettingsCard
                        Command="{Binding CreateHotkeyCommand}"
                        Header="{l:Static resx:Resources.HotkeysPage_CreateNew}"
                        IsClickEnabled="True"
                        Style="{DynamicResource AccentSettingsCard}">
                        <ui:SettingsCard.HeaderIcon>
                            <ui:FontIcon Glyph="&#xe710;" />
                        </ui:SettingsCard.HeaderIcon>
                    </ui:SettingsCard>

                    <ItemsControl
                        ItemTemplate="{StaticResource HotkeyTemplate}"
                        ItemsSource="{Binding HotkeysList, UpdateSourceTrigger=PropertyChanged}"
                        VirtualizingPanel.IsVirtualizing="True"
                        VirtualizingPanel.VirtualizationMode="Recycling">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <ikw:SimpleStackPanel Orientation="Vertical" Spacing="3" />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                    </ItemsControl>
                </ikw:SimpleStackPanel>
            </ikw:SimpleStackPanel>
        </ikw:SimpleStackPanel>
    </StackPanel>
</Page>