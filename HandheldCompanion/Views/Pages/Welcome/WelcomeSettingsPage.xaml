<Page
    x:Class="HandheldCompanion.Views.Pages.Welcome.WelcomeSettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:HandheldCompanion.Behaviors"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:l="clr-namespace:HandheldCompanion.Localization"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:viewmodels="clr-namespace:HandheldCompanion.ViewModels.Windows"
    Title="Customize"
    MaxWidth="1280"
    Margin="48"
    VerticalAlignment="Center"
    d:Background="White"
    d:DataContext="{d:DesignInstance Type=viewmodels:WelcomeViewModel}"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    mc:Ignorable="d">

    <StackPanel>
        <TextBlock
            Margin="0,0,0,8"
            behaviors:TextBlockUpperCase.IsUpperCase="True"
            Style="{StaticResource CapitalAccentTextBlockStyle}"
            Text="CUSTOMIZE THE EXPERIENCE" />

        <ikw:SimpleStackPanel Spacing="28">

            <!--  Header  -->
            <ikw:SimpleStackPanel Spacing="8">
                <TextBlock
                    Style="{StaticResource SubtitleBoldTextBlockStyle}"
                    Text="Confirm your defaults."
                    TextWrapping="Wrap" />
                <TextBlock
                    Style="{StaticResource BodyTextBlockStyle}"
                    Text="Recommended choices are pre-set where HC already has defaults. You can fine-tune everything later in Settings."
                    TextWrapping="Wrap" />
            </ikw:SimpleStackPanel>

            <!--  Content  -->
            <ikw:SimpleStackPanel Spacing="3">
                <ui:SettingsCard Description="Open Handheld Companion when Windows starts." Header="Launch HC at startup">
                    <ui:SettingsCard.HeaderIcon>
                        <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xED35;" />
                    </ui:SettingsCard.HeaderIcon>
                    <ui:ToggleSwitch IsOn="{Binding StartWithWindows, Mode=TwoWay}" />
                </ui:SettingsCard>

                <ui:SettingsCard Description="Start HC in the background after launch." Header="Open in background">
                    <ui:SettingsCard.HeaderIcon>
                        <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xEE40;" />
                    </ui:SettingsCard.HeaderIcon>
                    <ui:ToggleSwitch IsOn="{Binding StartMinimized, Mode=TwoWay}" />
                </ui:SettingsCard>

                <ui:SettingsCard Description="Resume faster and preserve the handheld experience around sleep and wake." Header="Enhanced sleep">
                    <ui:SettingsCard.HeaderIcon>
                        <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xE708;" />
                    </ui:SettingsCard.HeaderIcon>
                    <ui:ToggleSwitch IsOn="{Binding EnhancedSleep, Mode=TwoWay}" />
                </ui:SettingsCard>

                <ui:SettingsCard Description="Show Windows notifications for important HC events." Header="Toast notifications">
                    <ui:SettingsCard.HeaderIcon>
                        <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xE7F4;" />
                    </ui:SettingsCard.HeaderIcon>
                    <ui:ToggleSwitch IsOn="{Binding ToastNotifications, Mode=TwoWay}" />
                </ui:SettingsCard>

                <ui:SettingsCard Description="Help improve Handheld Companion by sending anonymous usage data." Header="Telemetry">
                    <ui:SettingsCard.HeaderIcon>
                        <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xE774;" />
                    </ui:SettingsCard.HeaderIcon>
                    <ui:ToggleSwitch IsOn="{Binding TelemetryEnabled, Mode=TwoWay}" />
                </ui:SettingsCard>

                <ui:SettingsCard Description="Used at startup and adjustable later." Header="HC system priority">
                    <ui:SettingsCard.HeaderIcon>
                        <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.SpeedHigh}" />
                    </ui:SettingsCard.HeaderIcon>
                    <ComboBox SelectedIndex="{Binding ProcessPriority, Mode=TwoWay}">
                        <ComboBoxItem Content="{l:Static resx:Resources.SettingsPage_Priority_Normal}" />
                        <ComboBoxItem Content="{l:Static resx:Resources.SettingsPage_Priority_AboveNormal}" />
                        <ComboBoxItem Content="{l:Static resx:Resources.SettingsPage_Priority_High}" />
                    </ComboBox>
                </ui:SettingsCard>

                <ui:SettingsExpander Description="Choose the look HC should use after onboarding. These options match the existing Settings page." Header="Theme">
                    <ui:SettingsExpander.HeaderIcon>
                        <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xE790;" />
                    </ui:SettingsExpander.HeaderIcon>
                    <ui:SettingsExpander.Items>
                        <ui:SettingsCard Description="Follow Windows or force a light/dark HC theme." Header="Application theme">
                            <ui:SettingsCard.HeaderIcon>
                                <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xE790;" />
                            </ui:SettingsCard.HeaderIcon>
                            <ComboBox SelectedIndex="{Binding MainWindowTheme, Mode=TwoWay}">
                                <ComboBoxItem Content="{l:Static resx:Resources.SettingsPage_ThemeDefault}" />
                                <ComboBoxItem Content="{l:Static resx:Resources.SettingsPage_ThemeLight}" />
                                <ComboBoxItem Content="{l:Static resx:Resources.SettingsPage_ThemeDark}" />
                            </ComboBox>
                        </ui:SettingsCard>

                        <ui:SettingsCard Description="Choose the window background effect." Header="Application backdrop">
                            <ui:SettingsCard.HeaderIcon>
                                <ui:FontIcon FontFamily="{DynamicResource SymbolThemeFontFamily}" Glyph="&#xE790;" />
                            </ui:SettingsCard.HeaderIcon>
                            <ComboBox SelectedIndex="{Binding MainWindowBackdrop, Mode=TwoWay}">
                                <ComboBoxItem Content="None" />
                                <ComboBoxItem Content="Mica" />
                                <ComboBoxItem Content="Tabbed" />
                                <ComboBoxItem Content="Acrylic" />
                            </ComboBox>
                        </ui:SettingsCard>
                    </ui:SettingsExpander.Items>
                </ui:SettingsExpander>
            </ikw:SimpleStackPanel>
        </ikw:SimpleStackPanel>
    </StackPanel>
</Page>
