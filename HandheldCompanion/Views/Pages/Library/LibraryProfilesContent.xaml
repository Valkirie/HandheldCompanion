<UserControl
    x:Class="HandheldCompanion.Views.Pages.Library.LibraryProfilesContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:behaviors="clr-namespace:HandheldCompanion.Behaviors"
    xmlns:controls="clr-namespace:HandheldCompanion.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:viewmodels="clr-namespace:HandheldCompanion.ViewModels"
    d:DataContext="{d:DesignInstance Type=viewmodels:LibraryPageViewModel}"
    d:DesignHeight="1080"
    d:DesignWidth="1920"
    mc:Ignorable="d">

    <ikw:SimpleStackPanel Spacing="8">
        <ui:ProgressBar
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top"
            IsIndeterminate="{Binding IsInitializing}"
            Visibility="{Binding IsInitializing, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <ItemsControl
            x:Name="profilesRepeater"
            HorizontalAlignment="Stretch"
            ItemsPanel="{Binding ProfilesCardsItemsPanel}"
            ItemsSource="{Binding ProfilesCardsItemsSource}"
            VirtualizingPanel.IsVirtualizing="True"
            VirtualizingPanel.VirtualizationMode="Recycling"
            Visibility="{Binding ShowProfilesCards, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                    <Setter Property="Margin" Value="0" />
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding DataContext.IsListView, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                            <Setter Property="Margin" Value="0,0,0,6" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding IsLiked}" Value="True">
                            <Setter Property="controls:JustifiedWrapPanel.ItemSpan" Value="3" />
                        </DataTrigger>
                        <DataTrigger Binding="{Binding DataContext.IsWideView, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                            <Setter Property="controls:JustifiedWrapPanel.ItemSpan" Value="3" />
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ContentControl
                        Margin="0"
                        HorizontalAlignment="Stretch"
                        Content="{Binding}">
                        <ContentControl.Style>
                            <Style TargetType="ContentControl">
                                <Style.Triggers>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsLiked}" Value="True" />
                                            <Condition Binding="{Binding DataContext.IsGridView, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="ContentTemplate" Value="{StaticResource LibraryTemplateWide}" />
                                    </MultiDataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsLiked}" Value="False" />
                                            <Condition Binding="{Binding DataContext.IsGridView, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="ContentTemplate" Value="{StaticResource LibraryTemplate}" />
                                    </MultiDataTrigger>
                                    <DataTrigger Binding="{Binding DataContext.IsListView, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="ContentTemplate" Value="{StaticResource LibraryTemplateList}" />
                                        <Setter Property="Height" Value="64" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding DataContext.IsWideView, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="ContentTemplate" Value="{StaticResource LibraryTemplateWide}" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </ContentControl.Style>
                    </ContentControl>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>

        <ItemsControl ItemsSource="{Binding CollectionGroups}" Visibility="{Binding ShowGroupedProfilesList, Converter={StaticResource BooleanToVisibilityConverter}}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <ikw:SimpleStackPanel Margin="0,0,0,28">
                        <Grid MaxHeight="31" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBlock
                                VerticalAlignment="Bottom"
                                Style="{StaticResource BaseTextBlockStyle}"
                                Text="{Binding Name}" />

                            <Button
                                Grid.Column="1"
                                VerticalAlignment="Stretch"
                                Command="{Binding DeleteCommand}"
                                Visibility="{Binding CanDelete, Converter={StaticResource BooleanToVisibilityConverter}}">
                                <ui:FontIcon Icon="{x:Static ui:SegoeFluentIcons.Delete}" />
                            </Button>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Profiles}">
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ContentPresenter">
                                    <Setter Property="HorizontalAlignment" Value="Stretch" />
                                    <Setter Property="Margin" Value="0,0,0,3" />
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <ContentControl
                                        HorizontalAlignment="Stretch"
                                        Content="{Binding}"
                                        ContentTemplate="{StaticResource LibraryTemplateList}" />
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ikw:SimpleStackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </ikw:SimpleStackPanel>
</UserControl>
