<common:GamepadWindow
    x:Class="HandheldCompanion.Views.Windows.WelcomeWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:common="clr-namespace:HandheldCompanion.Views.Classes"
    xmlns:controls="clr-namespace:HandheldCompanion.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:ikw="http://schemas.inkore.net/lib/ui/wpf"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.inkore.net/lib/ui/wpf/modern"
    xmlns:viewmodels="clr-namespace:HandheldCompanion.ViewModels"
    Title="Welcome to Handheld Companion"
    Width="1280"
    Height="720"
    MinWidth="640"
    MinHeight="480"
    d:DataContext="{d:DesignInstance Type=viewmodels:WelcomeViewModel}"
    d:Visibility="Visible"
    ui:ThemeManager.IsThemeAware="True"
    ui:TitleBar.ExtendViewIntoTitleBar="True"
    ui:TitleBar.Height="48"
    ui:WindowHelper.ApplyNoise="{Binding WelcomeWindowApplyNoise, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}"
    ui:WindowHelper.SystemBackdropType="Mica"
    ui:WindowHelper.UseModernWindowStyle="True"
    Icon="/Resources/icon.png"
    Loaded="Window_Loaded"
    SnapsToDevicePixels="True"
    Topmost="True"
    UseLayoutRounding="True"
    WindowStartupLocation="CenterScreen"
    WindowState="Normal"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="64" />
        </Grid.RowDefinitions>

        <ui:NavigationView
            x:Name="navView"
            IsBackButtonVisible="Collapsed"
            IsSettingsVisible="False"
            ItemInvoked="navView_ItemInvoked"
            PaneDisplayMode="Top"
            SelectionFollowsFocus="Enabled">
            <ui:NavigationView.MenuItems>
                <ui:NavigationViewItem Content="Welcome" Tag="Welcome" />
                <ui:NavigationViewItem Content="Device" Tag="Device" />
                <ui:NavigationViewItem Content="Software" Tag="OEM" />
                <ui:NavigationViewItem Content="Customize" Tag="Settings" />
                <ui:NavigationViewItem Content="Done" Tag="Done" />
            </ui:NavigationView.MenuItems>

            <ui:NavigationView.Content>
                <ui:ScrollViewerEx PanningMode="VerticalFirst">
                    <ui:Frame
                        Name="ContentFrame"
                        Width="{Binding ActualWidth, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type FrameworkElement}}}"
                        MaxWidth="2000" />
                </ui:ScrollViewerEx>
            </ui:NavigationView.Content>
        </ui:NavigationView>

        <Border
            Grid.Row="1"
            Background="{DynamicResource NavigationViewContentBackground}"
            BorderBrush="{DynamicResource NavigationViewContentGridBorderBrush}"
            BorderThickness="{DynamicResource NavigationViewContentGridBorderThickness}">
            <Grid Margin="24,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <ikw:SimpleStackPanel
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="16">
                    <TextBlock
                        VerticalAlignment="Center"
                        Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                        Text="Use Back and Continue to move through setup." />
                </ikw:SimpleStackPanel>

                <ikw:SimpleStackPanel
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Orientation="Horizontal"
                    Spacing="8">
                    <Button
                        x:Name="BackButton"
                        Height="42"
                        Click="BackButton_Click"
                        Content="Back" />
                    <Button
                        Height="42"
                        Click="SkipButton_Click"
                        Content="Skip" />
                    <Button
                        x:Name="ContinueButton"
                        Height="42"
                        Click="ContinueButton_Click">
                        <Button.Style>
                            <Style BasedOn="{StaticResource AccentButtonStyle}" TargetType="Button">
                                <Setter Property="Content" Value="Continue" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsFirstPage}" Value="True">
                                        <Setter Property="Content" Value="Get started" />
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding IsLastPage}" Value="True">
                                        <Setter Property="Content" Value="Start HC" />
                                    </DataTrigger>
                                    <MultiDataTrigger>
                                        <MultiDataTrigger.Conditions>
                                            <Condition Binding="{Binding IsLastPage}" Value="True" />
                                            <Condition Binding="{Binding RestartRequired}" Value="True" />
                                        </MultiDataTrigger.Conditions>
                                        <Setter Property="Content" Value="Restart" />
                                    </MultiDataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </ikw:SimpleStackPanel>
            </Grid>
        </Border>
    </Grid>
</common:GamepadWindow>
