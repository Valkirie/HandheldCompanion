namespace ControllerCommon.Utils
{
    public enum HIDmode
    {
        Xbox360Controller = 0,
        DualShock4Controller = 1,
        NoController = 2,
    }

    public enum HIDstatus
    {
        Disconnected = 0,
        Connected = 1
    }
}
