namespace ControllerCommon.Utils
{
    public enum HIDmode
    {
        DualShock4Controller = 0,
        Xbox360Controller = 1,
        NoController = 2,
    }

    public enum HIDstatus
    {
        Disconnected = 0,
        Connected = 1
    }
}
